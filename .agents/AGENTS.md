# Agent Rules

- NEVER modify, stage, commit, or push `library/src/main/resources/application.properties`. Always keep the user's local version of this file untouched to avoid overwriting database and mail configurations.
